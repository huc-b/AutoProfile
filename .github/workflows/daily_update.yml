name: Update Dashboard

on:
  schedule:
    # 北京时间每天上午 8:00 运行 (UTC 时间 00:00)
    - cron: '0 0 * * *'
  workflow_dispatch: # 允许手动运行测试

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        pip install -r requirements.txt

    - name: Run script
      run: python main.py

    - name: Commit and Push
      run: |
        # ⚠️⚠️⚠️ 请修改下面两行！⚠️⚠️⚠️
        git config --global user.email "huc.a5411@gmail.com"
        git config --global user.name "huc-b"
        
        git add README.md
        # 如果有变动才提交
        git diff --quiet && git diff --staged --quiet || (git commit -m "📊 Update Dashboard: $(date +'%Y-%m-%d')" && git push)
